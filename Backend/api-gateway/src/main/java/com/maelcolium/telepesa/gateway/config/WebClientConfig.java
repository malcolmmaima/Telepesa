package com.maelcolium.telepesa.gateway.config;

import org.springframework.context.annotation.Configuration;

/**
 * WebClient Configuration for API Gateway
 * 
 * This configuration is now empty to avoid duplicate WebClient bean definitions.
 * The WebClient bean is defined in GatewayConfig.java
 * 
 * @author Telepesa Development Team
 * @version 1.0.0
 */
@Configuration
public class WebClientConfig {
    // WebClient bean moved to GatewayConfig to avoid duplicates
} 